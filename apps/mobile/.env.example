# Copy to .env — Expo inlines EXPO_PUBLIC_* at build time.
# Use your machine's LAN IP (not localhost) when running on a physical device.
EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_AUTH_TOKEN=dev-token
