import { Text, View } from 'react-native';

// Placeholder home route so the app bundles. Replace with the real Add screen.
export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Yaadora</Text>
    </View>
  );
}
