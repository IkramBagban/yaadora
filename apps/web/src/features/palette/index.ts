/**
 * cmd-K palette + unified search (P-8, P-9). Public surface of the feature —
 * everything else in this folder is internal.
 */
export { CommandPalette } from './CommandPalette'
export { openPalette, closePalette, togglePalette } from './paletteBus'
